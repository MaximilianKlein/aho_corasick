name: aho_corasick
description: Pattern Matching implementation of aho_corasick using a state machine to find all matches in a text for a large number of words.
version: 2.0.0
homepage: https://github.com/MaximilianKlein/aho_corasick

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:

dev_dependencies:
  pedantic: ^1.0.0
  test: ^1.0.0
